# 小芽 · 更新日志

本项目遵循语义化版本号，每次发版必须同步更新：`app/build.gradle.kts` 中的 `versionCode`/`versionName`、`ui/AppConfig.kt` 中的 `VERSION_CODE`/`VERSION_NAME`/`CHANGELOG`、仓库根目录 `version.json`，并在此追加一条更新说明。

## v1.1.0（2026-08-14）

- 新增：录音记录——记录详情页可录一段孩子的声音（第一次叫妈妈、唱儿歌、背古诗），支持播放与删除。
- 新增：桌面小组件——手机桌面放一块「今日完成 + 成长天数 + 成长树叶子」的小组件，数据随 App 实时同步。
- 新增：深色模式——设置页可切换「跟随系统 / 浅色 / 深色」，睡前记录更护眼。
- 新增：每日本地通知提醒——按设定时间推送「睡前提醒」，无需打开 App。

## v1.0.7（2026-08-14）

- 新增：首页显示孩子精确年龄（X岁X月X天）与下次生日倒计时。
- 新增：数据洞察「成长里程碑」时间轴，把成长瞬间串成一条线。
- 新增：数据洞察「打卡热力图」，近 15 周打卡情况一目了然。
- 新增：记录详情「分享成长卡」，一键生成含照片与文字的卡片分享。

## v1.0.6（2026-08-14）

- 新增：记录详情页「补一张照片」按钮，可随时拍照或从相册补照片（无需走三步新增流程）。
- 新增：设置页「清除记录」，一键清空全部记录与照片（保留个人档案与分类），方便新用户清掉示例数据。

## v1.0.5（2026-08-14）

- 新增：记录时可选择「拍照」或「从相册选照片」，方便拍下孩子洗漱、作品、亲子瞬间等真实画面。

## v1.0.4（2026-08-14）

- 更新源从 GitHub 切换到腾讯云 COS（国内极速直连下载），并加入阿里云 Maven 镜像加速编译。

## v1.0.3（2026-08-14）

- 修复：计划页下拉到底时，「当天记录」与「即将提醒」因同一列表的 key 重复导致的崩溃（`Key "rXX" was already used`）。

## v1.0.2（2026-08-14）

- 新增：「检查更新」支持 App 内直接下载 APK 并唤起系统安装器，无需跳转浏览器。

## v1.0.1（2026-08-14）

- 修复：计划页「时间线」纵向连接线使用 `weight` 导致的下拉崩溃。

## v1.0.0（2026-08-14）

首版上线：

- 新增「作息、阅读、学习任务、成长记录、身高体重、兴趣课、亲子活动、物品清单」八大模块，统一记录卡支持新增 / 编辑 / 删除 / 状态切换。
- 首页场景主视觉（绘本成长册 + 家庭学习角）、今日优先、核心数据、快捷记录、最近内容、成长树与阶段成果。
- 记录库支持关键字搜索、类型 / 状态 / 标签 / 时间筛选、按日期或数值排序。
- 成长日历：月历 + 当日时间线 + 即将提醒（按「下一次日期」动态计算）。
- 数据洞察：完成趋势、分类占比、身高曲线、近期变化与可解释建议，图表标注数据来源。
- 成长相册：按月份组织，点击查看大图、切换、返回关联记录。
- 三步新增 / 编辑（选类型 → 填字段 → 传照片并确认），支持本地图片上传、失败重试与封面设置。
- 我的：档案编辑、每日提醒、自建分类（即时同步到筛选 / 图例 / 表单）、数据导出与本地存储说明。
- 原创品牌 Logo、应用图标、13 张场景插画与空状态插画。
- 完成亲子活动后成长树长出新叶并弹出鼓励卡。
- 本地 JSON 持久化，刷新后保留全部修改。
